{{!-- User Dashboard --}}
<div class="mt-4 ms-5">
  <div>
    <h2>Welcome, {{user.first_name}}!</h2>
  </div>
</div>

{{!-- Where the jobs are--}}

{{!-- FORM LAYOUT for creating the job card --}}
<div class="job-create">
  <form class="form new-project-form">
    <div class="form-group">
      <label for="job-title">Job Title:</label>
      <input class="form-input" type="text" id="job-title" name="job-title" />
    </div>

    <div class="form-group">
      <label for="job-desc">Description:</label>
      <textarea class="form-input" id="job-desc" name="job-desc"></textarea>
    </div>
     <div class="">
      
      <input class="form-input form-control-lg col-3" placeholder="Salary" type="text" id="job-salary" name="job-salary"></input>
    </div>
      <div class="">

      <input class="form-input form-control-lg col-3" placeholder="Status" type="text" id="job-status" name="job-status"></input>
    </div>
      <div class="">

      <input class="form-input form-control-lg col-3" placeholder="Url" type="text" id="job-url" name="job-url"></input>
    </div>
     <div class="">

      <input class="form-input form-control-lg col-3" placeholder="Location" type="text" id="job-location" name="job-location"></input>
    </div>
     <div class="">
      
      <textarea class="form-input form-control-lg col-lg-3" placeholder="Description"id="job-desc" name="job-desc"></textarea>
    </div>
    <div class="">
      <button type="submit" id="job-button" class="btn btn-primary">Create</button>
    </div>
    </div>
</div>
</form>


{{!-- CARD LAYOUT --}}
{{#if user.jobs.length}}
<div class="container mt-5">
<div><h1  class="text-center">Applied</h1>
{{#each user.jobs as |job| }}
{{!-- {{#if user.jobs.status === "applied"}} --}}
<div class="card text-white bg-info  mb-3" style="max-width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{job.title}}</h5>
     <h6 class="card-subtitle mb-2 text-light">{{job.company}}</h6>
     <p class="card-text">{{job.location}}</p>
     <p class="card-text">{{job.description}}</p>
      <p class="card-text">{{job.salary}}</p>
      <a  href="{{job.url}}" class="card-link text-light">{{job.url}}</a>
 
 <select class="form-select form-select-sm  mt-3 mb-3"  class="dropdown-menu" aria-label=".form-select-lg example">
  <option selected>Select a status</option>
  <option value="Applied">Applied</option>
  <option value="Interviewing">Interviewing</option>
  <option value="Onboarding">Onboarding</option>
  <option value="Rejected">Rejected</option>
</select>

  </div>
</div>
{{!-- {{/if}} --}}
{{/each}}</div>

<div ><h1  class="text-center">Interviewing</h1>
{{!-- Created each individual card --}}
{{#each user.jobs as |job| }}
{{!-- {{#if user.jobs.status === "applied"}} --}}
<div class="card text-white bg-info mb-3" style="max-width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{job.title}}</h5>
     <h6 class="card-subtitle mb-2 text-light">{{job.company}}</h6>
     <p class="card-text">{{job.location}}</p>
     <p class="card-text">{{job.description}}</p>
      <p class="card-text">{{job.salary}}</p>
      <a  href="{{job.url}}" class="card-link text-light">{{job.url}}</a>
 
 <select class="form-select form-select-sm  mt-3 mb-3"  class="dropdown-menu" aria-label=".form-select-lg example">
  <option selected >Select a status</option>
  <option value="Applied">Applied</option>
  <option value="Interviewing">Interviewing</option>
  <option value="Onboarding">Onboarding</option>
  <option value="Rejected">Rejected</option>
</select>

  </div>
</div>
{{!-- {{/if}} --}}
{{/each}}
</div>
<div><h1 class="text-center">Onboarding</h1>
{{!-- Created each individual card --}}
{{#each user.jobs as |job| }}
{{!-- {{#if user.jobs.status === "applied"}} --}}
<div class="card text-white bg-info mb-3" style="max-width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{job.title}}</h5>
     <h6 class="card-subtitle mb-2 text-light">{{job.company}}</h6>
     <p class="card-text">{{job.location}}</p>
     <p class="card-text">{{job.description}}</p>
      <p class="card-text">{{job.salary}}</p>
      <a  href="{{job.url}}" class="card-link text-light">{{job.url}}</a>
 
 <select class="form-select form-select-sm mt-3 mb-3"  class="dropdown-menu" aria-label=".form-select-lg example">
  <option selected >Select a status</option>
  <option value="Applied">Applied</option>
  <option value="Interviewing">Interviewing</option>
  <option value="Onboarding">Onboarding</option>
  <option value="Rejected">Rejected</option>
</select>

  </div>
</div>
{{!-- {{/if}} --}}
{{/each}}
</div>

<div><h1 class="text-center">Rejected</h1>
{{!-- Created each individual card --}}
{{#each user.jobs as |job| }}
{{!-- {{#if user.jobs.status === "applied"}} --}}
<div class="card text-white bg-danger mb-3" style="max-width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{job.title}}</h5>
     <h6 class="card-subtitle mb-2 text-light">{{job.company}}</h6>
     <p class="card-text">{{job.location}}</p>
     <p class="card-text">{{job.description}}</p>
      <p class="card-text">{{job.salary}}</p>
      <a  href="{{job.url}}" class="card-link text-light">{{job.url}}</a>
 
 <select class="form-select form-select-sm  mt-3 mb-3"   onchange="show()"class="dropdown-menu" aria-label=".form-select-lg example">
  <option selected >Select a status</option>
  <option value="Applied">Applied</option>
  <option value="Interviewing">Interviewing</option>
  <option value="Onboarding">Onboarding</option>
  <option value="Rejected">Rejected</option>
</select>

  </div>
</div>
{{!-- {{/if}} --}}
{{/each}}
</div>
</div>
{{/if}}

<script src="./js/profile.js"></script>